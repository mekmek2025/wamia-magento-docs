import Head from "next/head"; import Link from "next/link"; import { getAllDocPaths, getDocData, getDocStructure } from "../../lib/docs";
export async function getStaticPaths() { const paths = getAllDocPaths(); return { paths, fallback: false }; } export async function getStaticProps({ params }) { const docData = await getDocData(params.slug); const structure = getDocStructure(); return { props: { docData, structure } }; }
export default function DocPage({ docData, structure }) { return (<div className="flex min-h-screen bg-gray-50"><Head><title>{docData.title || "Documentation"} - Wamia Magento Docs</title></Head><nav className="w-64 bg-white border-r border-gray-200 h-screen overflow-y-auto p-4"><Link href="/" className="text-lg font-bold text-blue-600">Wamia Docs</Link></nav><main className="flex-1 max-w-4xl mx-auto py-8 px-6"><article className="prose prose-lg max-w-none"><div dangerouslySetInnerHTML={{ __html: docData.contentHtml }} /></article></main></div>); }
